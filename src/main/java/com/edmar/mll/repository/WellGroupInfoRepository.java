package com.edmar.mll.repository;

import com.edmar.mll.entities.dto.WellGroupInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WellGroupInfoRepository extends JpaRepository<WellGroupInfo,Integer> {
}
