package com.edmar.mll.repository;

import com.edmar.mll.entities.dto.WellInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WellInfoRepository extends JpaRepository<WellInfo,Integer> {
}
